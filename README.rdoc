== Welcome

This is a rather particular gem for doing directory lookups and authentication using the Williams College LDAP server. It is probably not very useful to very broad audience, particularly since the LDAP server requires connections come from on-campus.
